# Agentic Patterns

This directory contains examples of agentic patterns implemented in both TypeScript and Go.

## JavaScript/TypeScript

To run the JavaScript/TypeScript examples:

```bash
cd agentic-patterns
npm i
npm start
```

## Go

To run the Go examples:

```bash
cd agentic-patterns/go
go get
genkit start -- go run main.go
